import { LoadersPageContent } from '@/app/(image)/loaders/components/pageContent';

export const metadata = {
  title: 'Loaders',
};

export default function Loaders() {
  return <LoadersPageContent />;
}
